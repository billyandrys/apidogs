const { Router } = require("express");

const router = Router();

// const {
//   getAllCreateTemperament,
// } = require("../controllers/cretaAllTemperaments");

// router.get("/", getAllCreateTemperament);

module.exports = router;
